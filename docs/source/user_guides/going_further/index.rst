Going further
==============

The more technical material, shared by several of the modeling approaches above: how lifetime
observations are encoded when a failure date is only known to fall inside a window, the
likelihood every parametric model is fitted with, the reward and discounting framework used
to attach costs to events, and the interface every maintenance policy exposes.

.. toctree::
    :maxdepth: 1

    interval_censoring
    likelihood
    reward_framework
    from_process_to_policy
