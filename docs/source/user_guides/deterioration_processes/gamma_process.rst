Gamma process
==============

Not implemented yet.

A gamma process models the deterioration level itself as an increasing random process, so the
event of interest is the crossing of a failure threshold. It needs deterioration measurements
(a measured level and the date it was measured, repeated over the asset's life) rather than
failure dates.
