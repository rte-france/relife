Lifetime modeling
==================

Lifetime modeling covers assets that fail once and are then **replaced**: what is modeled is
a duration, and the asset is as good as new after the event. This section goes from raw
failure-time data (with censoring and truncation) to a lifetime model, from that model to a
replacement policy, and from a model back to synthetic data.

.. toctree::
    :maxdepth: 1

    datasets
    censoring_and_truncation
    non_parametric_models
    distributions
    regressions
    renewal_theory
    run_to_failure
    preventive_age_replacement
    sampling
